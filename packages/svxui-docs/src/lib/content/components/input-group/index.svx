---
title: InputGroup
description: A component for combines multiple inputs like input, select or buttons into a single, cohesive input area for better layout and user interaction.
category: components
---

<script lang="ts">
    import ApiReference from '$lib/components/markdown/components/ApiReference.svelte';
    import SectionDemo from '$lib/components/markdown/components/SectionDemo.svelte';
    import { examples } from './examples/index.js'
</script>

<SectionDemo {...examples.overview }/>

<ApiReference category="components" names={['input-group', 'input-group-item']}/>

## Examples

{#each examples.items as item }
<SectionDemo {...item }/>
{/each}